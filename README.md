# tesr-repo

MedWe UAE Laravel website project.

## Setup

```bash
cd medweuae
composer install
cp .env.example .env   # create from your local .env values
php artisan key:generate
php artisan serve
```
